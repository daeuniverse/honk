# Repository skills

Start with the task-routing table in `AGENTS.md` and read the `SKILL.md` the route for your task names. Each skill says which rules files to read first.

Reading the file by path works with any client that can read the checkout. Native skill discovery depends on the client and is not assumed here.
